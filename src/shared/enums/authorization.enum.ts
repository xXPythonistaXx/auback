export enum PermissionAction {
  manage = 'manage',
  read = 'read',
  create = 'create',
  update = 'update',
  delete = 'delete',
}

export enum DefaultRoles {
  admin = 'admin',
  employee = 'employee',
  employer = 'employer',
}

export enum DefaultSubjects {
  all = 'all',
  subjects = 'subjects',
  permissions = 'permissions',
  roles = 'roles',
  refreshTokens = 'refresh_tokens',
  addresses = 'addresses',
  users = 'users',
  employees = 'employees',
  employers = 'employers',
  opportunities = 'opportunities',
  states = 'states',
  universities = 'universities',
  academicBackgrounds = 'academic_backgrounds',
  experiences = 'experiences',
  tools = 'tools',
  languages = 'languages',
  segments = 'segments',
  benefits = 'benefits',
  jobs = 'jobs',
  courses = 'courses',
}
